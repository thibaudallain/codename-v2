<div class="row d-flex justify-content-center mt-5">
  <div class="col-6 bg-light p-4">
    <%= simple_form_for @game do |f| %>
      <%= f.input :name %>
      <%= f.button :submit, "Crée une partie", class: "btn btn-success" %>
    <% end %>
  </div>
</div>
